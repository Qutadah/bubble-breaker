let x = 5;
console.log(x)


// define all heuristics as functions to scan the puzzle and find the bubble to click and send click event to yahoo dom?

// Input argument should be only the puzzle configuration. (puzzle_layout)


// find using all functions the element_id to click.

function h1(puzzle_layout) {

let element_id = null;

//scan all puzzle_layout and append the found bubble element ids and compare them and then decide which one.


    return bubble_to_click;
}
